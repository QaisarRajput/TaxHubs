{
  "name": "etl",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "corepack pnpm --filter @tax/schema build && tsc -p tsconfig.json --noEmit",
    "validate": "node dist/validate.js",
    "test": "vitest run src/validate.test.ts --passWithNoTests"
  },
  "dependencies": {
    "@tax/schema": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.10.1"
  }
}
